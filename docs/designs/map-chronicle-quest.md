---
status: PROMOTED
---
# CEO Plan: 동네 연대기 — 지도 성장 + 여행 코스 추천

Generated by /plan-ceo-review on 2026-07-22
Branch: main | Mode: SELECTIVE EXPANSION
Repo: softHyeonsung/sheriff-app
Design doc: `~/.gstack/projects/sheriff-app/user-main-design-20260722-165729.md` (office-hours session, 2026-07-22)

## Vision

### 10x Check / 12-Month Ideal
현재: 보안관 = 지도+피드+모임+보안관 뱃지, 5탭, MVP 방금 완료.
이번 계획: 지도 탭 "모임" 모드에 "동네 연대기" 레이어 추가 — 방문(게시물 기반) 랜드마크 성장 + 코스 추천.
12개월 이상적 상태: 지도가 단순 발견 도구를 넘어 개인+소셜 "성장" 아티팩트가 된다. 친구 지도와 비교하고 공유하는 재미가 보안관 신뢰 시스템(뱃지)과 결합되고, 코스 추천은 CLAUDE.md §11에 이미 적힌 "2단계 여행객" 비전으로 자연스럽게 확장되는 다리 역할을 한다.

## Scope Decisions

| # | Proposal | Effort | Decision | Reasoning |
|---|----------|--------|----------|-----------|
| 1 | 등급업 셀레브레이션 애니메이션 (마커 등급 상승 시 스케일+글로우) | S | DEFERRED | 데모 임팩트는 크지만 이번 라운드는 핵심 데이터모델(집계)과 코스 기능이 우선. 기존 뱃지 어워드 애니메이션 패턴을 그대로 재사용 가능해 나중에 저비용으로 추가 가능 — TODOS.md에 기록 |
| 2 | 동네 스카이라인 공유 이미지 (수집한 랜드마크를 하나의 스카이라인 이미지로 렌더링) | M | DEFERRED | 소셜 자랑 요소로 좋지만 렌더링 품질 리스크가 있어 코어 기능 검증 후 추가 — TODOS.md에 기록 |
| 3 | 친구 지도 나란히 비교 화면 | M | ACCEPTED | 사용자가 델라이트 요소로 직접 꼽음(office-hours 세션, "둘 다" 답변) — 핵심 스코프에 포함 |
| 4 | 코스 추천 = "빈칸 채우기" (아직 안 가본 근처 랜드마크 후보 우선 추천) | S-M | ACCEPTED | 코스 추천과 지도 꾸미기 게임화를 하나의 루프로 묶는 저비용 확장 — 핵심 스코프에 포함 |

## Accepted Scope (added to this plan's remaining review sections)
- 친구 지도 비교 — **기존 `app/shared-map/[uid].tsx` 확장** (11섹션 리뷰의 outside voice가 발견: 새 화면이 아니라 이미 있는 친구 지도 보기 화면에 랜드마크 등급 렌더링만 추가). 집계 기준: 개인 소유(내/상대방 각각의 게시물만 집계). 이번 라운드는 상대방 화면 하나만 — 나란히 동시 비교는 사용자가 명시적으로 후속 작업으로 분리(TODOS.md).
- "빈칸 채우기" 방식의 코스 추천 — 아직 방문 기록 없는 근처 랜드마크 후보를 우선순위로 코스에 편입

## Cross-Model Tension 해결 (11섹션 리뷰 + Outside Voice)
- **실제 공모전 제출일 확정: 2026-09-21** (문서에 날짜가 없었고, CLAUDE.md 1차 마감 5/25는 이미 지난 날짜였음 — outside voice가 발견)
- **"모임·퀘스트" 병합 모드는 코드에 없음** — 실제로는 "모임" 단일 모드만 존재. 이번 기능은 새 모드 없이 기존 "모임" 모드에 통합 (outside voice 발견 + 사용자 확정)
- **친구 지도 비교는 새 화면이 아니라 기존 `shared-map/[uid].tsx` 확장** (outside voice 발견 + 사용자 확정)

## NOT in Scope
- 등급업 셀레브레이션 애니메이션 — 핵심 집계/코스 기능이 우선 (TODOS.md)
- 동네 스카이라인 공유 이미지 — 렌더링 품질 리스크, 코어 기능 검증 후 (TODOS.md)
- 친구 지도 동시 비교 뷰(한 화면에서 나란히) — 사용자가 명시적으로 후속 작업으로 분리 (TODOS.md)
- Approach A(체크인 기반), Approach B(방문 원장+자동감지) — 대안 검토했으나 미선택
- 공동/사회적 랜드마크 집계(전체 방문자 합산) — 개인 소유 기본값으로 결정, 스트레치 목표로 보류
- 새 visit_log Firestore 컬렉션, 새 Cloud Function 트리거, 백그라운드 위치 추적 — 전부 회피 (공모전 일정 제약)

## What Already Exists (reused, not rebuilt)
- `app/(tabs)/index.tsx`: `subscribeFeedPosts()` 구독, `makeMarkerSrc`/`makeGatheringMarkerSrc` 패턴, `PIN_COLORS`, `dirLoading` 가드, `kakaoDirectionsFn`/`odsayDirectionsFn`, `DRAW_ROUTE_SEGMENTS`
- `src/api/posts.ts`: `location_pin` 필드 (장소 고유 식별자)
- `src/api/tourApi.ts`: `fetchNearbyTourSpots`
- `app/(tabs)/profile.tsx`: `myPosts` 클라이언트 필터링 패턴 (집계 훅이 그대로 따를 패턴)
- `app/shared-map/[uid].tsx`: 친구 지도 비교의 기반 화면
- DESIGN.md: 지도 에러 패턴, 빈 상태 패턴, 지도 핀 접근성 라벨 규칙

## Dream State Delta
12개월 이상적 상태(지도가 개인+소셜 성장 아티팩트, 스카이라인 공유, 동시 비교, 등급업 셀레브레이션)까지 가는 첫 걸음. 이번 계획은 개인 랜드마크 성장 + 단방향 친구 지도 보기 + 코스 추천까지만 커버한다. 나머지는 TODOS.md에 순서대로 쌓여 있다.

## Error & Rescue Registry
| METHOD/CODEPATH | WHAT CAN GO WRONG | EXCEPTION | RESCUED? | RESCUE ACTION | USER SEES |
|---|---|---|---|---|---|
| 게시물 집계 훅 (subscribeFeedPosts 재사용) | 네트워크 오류로 구독 실패 | Firestore SDK error | Y (신규 결정) | DESIGN.md 에러 패턴 적용 | "지도를 불러올 수 없어요" + 재시도 버튼 |
| 코스 빌더 (kakaoDirectionsFn 체이닝) | Kakao Directions API 타임아웃/오류 | 기존 catch(e) 패턴 재사용 | Y (기존) | Alert.alert | "경로 탐색 중 문제가 발생했어요" (기존 문구 재사용) |
| 친구 지도 비교 (shared-map 확장) | 상대방 프로필/게시물 로드 실패 | getUserProfile/loadSavedPins 실패 | 기존 코드 처리 그대로 (신규 갭 아님) | 기존 로직 유지 | 기존 처리 그대로 |
| 등급 계산 | location_pin.id 그룹에 게시물 0건 | N/A | Y | 마커 자체를 렌더링하지 않음 | 핀이 안 보임 (의도된 동작) |

## Failure Modes Registry
| CODEPATH | FAILURE MODE | RESCUED? | TEST? | USER SEES | LOGGED? |
|---|---|---|---|---|---|
| 게시물 집계 구독 | 네트워크 끊김 | Y (신규) | N | 에러 UI + 재시도 | 기존 console.warn 패턴 재사용 |
| 코스 빌더 | 목적지 좌표 이상/API 오류 | Y (기존) | N | Alert | 기존 console.warn |
| 등급 계산 함수 | 경계값(0/1/6/7/14/15/29/30) | Y | Y (신규 테스트) | 마커 등급 정확히 표시 | N/A (순수 함수) |
| shared-map 확장 | 상대방 uid 없음/탈퇴 유저 | 기존 로직 (신규 아님) | N | 기존 처리 그대로 | 기존 그대로 |

CRITICAL GAP 없음 — 모든 신규 코드패스가 최소 하나의 대응(에러 UI, Alert, 또는 의도된 무표시)을 가진다.

## Diagrams

### System Architecture
```
[posts collection (Firestore)]
        │  onSnapshot (기존, 신규 읽기 없음)
        ▼
[subscribeFeedPosts() — 이미 index.tsx에 존재]
        │
        ▼
[신규: 클라이언트 그룹핑 훅]
   author_id별 필터 → location_pin.id별 그룹 → 게시물 수 → 등급 매핑
        │                              │
        ▼                              ▼
[신규: 마커 렌더러]              [shared-map/[uid].tsx 확장]
 (makeMarkerSrc 패턴 재사용)      (상대방 uid로 훅 재호출)
        │
        ▼
[지도 "모임" 모드 WebView] ← 기존 PIN_COLORS 파이프라인에 랜드마크 마커 추가

[신규: 코스 빌더]
   tourApi.ts(주변 후보) + 미방문 랜드마크 우선순위
        │
        ▼
   kakaoDirectionsFn 체이닝 (기존) → DRAW_ROUTE_SEGMENTS (기존)
```

### Data Flow (신규 집계 훅)
```
INPUT(posts state) ──▶ FILTER(author_id) ──▶ GROUP(location_pin.id) ──▶ MAP(count→tier) ──▶ RENDER(marker)
   │                        │                       │                        │                  │
   ▼                        ▼                       ▼                        ▼                  ▼
[구독 실패?]           [author_id 없음?]      [location_pin 없음?]      [count=0?]         [tier SVG 없음?]
 → 에러 UI+재시도       → 발생 불가             → 필터에서 제외            → 마커 미표시        → 발생 불가(상수 정의)
 (신규 처리)            (Firestore 필수 필드)    (설계상 명시)             (의도된 동작)         (컴파일 타임 보장)
```

## Stale Diagram Audit
이 플랜이 건드리는 파일들에 기존 ASCII 다이어그램 없음 (index.tsx, profile.tsx, shared-map/[uid].tsx 모두 다이어그램 주석 없이 작성됨) — 신선도 이슈 없음.

## Implementation Tasks
- [ ] **T1 (P1)** — 지도 서브뷰 — "모임" 모드 바텀시트 재사용 여부 확인
  - Surfaced by: Section 1/Sequencing (outside voice) — 시퀀싱 역전 발견
  - Files: `app/(tabs)/index.tsx`, `app/(tabs)/_layout.tsx`
  - Verify: 바텀시트 컴포넌트가 조건부 버튼 추가를 지원하는지 코드 확인
- [ ] **T2 (P1)** — 게시물 집계 훅 작성 (신규 쿼리 없이 기존 구독 재사용)
  - Surfaced by: Section 1 (아키텍처 리뷰에서 기존 패턴 불일치 발견)
  - Files: `app/(tabs)/index.tsx`, `src/api/posts.ts`
  - Verify: 유닛 테스트로 그룹핑 로직 검증
- [ ] **T3 (P1)** — 등급 임계값 상수 + 마커 SVG 5종 + a11y 라벨
  - Surfaced by: Section 11 (디자인/접근성 갭)
  - Files: `app/(tabs)/index.tsx`
  - Verify: 5개 등급 마커가 실제 기기에서 렌더링되는지 육안 확인
- [ ] **T4 (P1)** — 지도 에러 상태 UI 추가 (내 연대기 서브뷰)
  - Surfaced by: Section 1/2 (에러 경로 누락 발견)
  - Files: `app/(tabs)/index.tsx`
  - Verify: 네트워크 끄고 재시도 버튼 동작 확인
- [ ] **T5 (P2)** — 코스 빌더 함수 (nearest-neighbor + 빈칸 채우기)
  - Surfaced by: Open Question 3, CEO Plan 승인된 확장 항목
  - Files: `src/api/tourApi.ts`, `app/(tabs)/index.tsx`
  - Verify: 데모 시나리오로 2~4개 스팟 경로 확인
- [ ] **T6 (P2)** — shared-map/[uid].tsx 확장 (랜드마크 등급 렌더링)
  - Surfaced by: Outside Voice (기존 화면 재사용 발견)
  - Files: `app/shared-map/[uid].tsx`
  - Verify: 친구 프로필에서 진입 후 랜드마크 마커 표시 확인
- [ ] **T7 (P2)** — 등급 계산 순수 함수 분리 + 경계값 테스트
  - Surfaced by: Section 6 (테스트 커버리지 결정)
  - Files: 신규 유틸 파일, `__tests__/`
  - Verify: `npm test` 통과

## Completion Summary
```
+====================================================================+
|            MEGA PLAN REVIEW — COMPLETION SUMMARY                   |
+====================================================================+
| Mode selected        | SELECTIVE EXPANSION                          |
| System Audit         | 기존 auth 관련 TODOS 3건, 관련 없음 확인      |
| Step 0               | 대안 C(동네 연대기) 선택, 확장 2건 수락       |
| Section 1  (Arch)    | 2 issues found (집계 패턴 불일치, 에러 경로) |
| Section 2  (Errors)  | 1 gap found (구독 실패 무대응) → 수정        |
| Section 3  (Security)| 0 issues — posts는 이미 전체 공개 read       |
| Section 4  (Data/UX) | 2 edge cases (더블탭, 빈 비교) → 기존 재사용 |
| Section 5  (Quality) | 0 issues (수정 후 기존 패턴과 일치)          |
| Section 6  (Tests)   | 1 gap → 등급 함수 테스트 추가 결정           |
| Section 7  (Perf)    | 0 issues (신규 쿼리/인덱스 불필요)           |
| Section 8  (Observ)  | 0 issues (기존 로깅 관례 + 에러 UI로 커버)   |
| Section 9  (Deploy)  | 0 issues (마이그레이션 없음, 순수 추가)      |
| Section 10 (Future)  | Reversibility: 5/5, debt: 낮음               |
| Section 11 (Design)  | 1 issue found (마커 시각 언어) → 결정        |
+--------------------------------------------------------------------+
| NOT in scope         | written (6 items)                            |
| What already exists  | written                                       |
| Dream state delta     | written                                      |
| Error/rescue registry | 4 methods, 0 CRITICAL GAPS                   |
| Failure modes         | 4 total, 0 CRITICAL GAPS                     |
| TODOS.md updates      | 3 items (셀레브레이션 애니메이션, 스카이라인 공유, 동시 비교) |
| Scope proposals       | 4 proposed, 2 accepted (SELECTIVE EXPANSION) |
| CEO plan              | written                                      |
| Outside voice         | ran (Claude subagent, Codex 미설치) — 3개 텐션 발견/해결 |
| Diagrams produced     | 2 (system architecture, data flow)           |
| Stale diagrams found  | 0                                             |
| Unresolved decisions  | 0                                             |
+====================================================================+
```

### Unresolved Decisions
없음 — 모든 AskUserQuestion에 응답 완료.

## Deferred to TODOS.md
- 등급업 셀레브레이션 애니메이션 — 뱃지 어워드 스펙(스케일 0.8→1.1→1.0 + 골드 글로우) 재사용, effort S
- 동네 스카이라인 공유 이미지 — 소셜 공유용 렌더링, effort M
- 친구 지도 동시 비교 뷰 — 한 화면에서 나란히 비교, effort M
